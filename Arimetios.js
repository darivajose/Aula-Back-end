let precoCarro = 80000;
let taxaDocumento = 500;
let total = precoCarro + taxaDocumento; // 80500


let desconto = 2000;
let precoFinal = precoCarro - desconto; // 78000

let quantidadeParcelas = 12;
let valorParcela = 1500;
let totalFinanciado = quantidadeParcelas * valorParcela; // 18000

let faturamentoMensal = 30000;
let diasTrabalhados = 20;
let mediaPorDia = faturamentoMensal / diasTrabalhados; // 1500

let resto = 10 % 3; // 1 (pois 10 dividido por 3 é 3 e sobra 1)

let base = 2;
let expoente = 3;
let resultado = base ** expoente; // 8 (2 * 2 * 2)


let contador = 0;
contador++; // Agora é 1
contador--; // Volta a ser 0

console.log(contador)